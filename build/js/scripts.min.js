(function() {
  'use strict';
  window.addEventListener('load', function() {
    // Fetch all the forms we want to apply custom Bootstrap validation styles to
    var forms = document.getElementsByClassName('needs-validation');
    // Loop over them and prevent submission
    var validation = Array.prototype.filter.call(forms, function(form) {
      form.addEventListener('submit', function(event) {
        if (form.checkValidity() === false) {
          event.preventDefault();
          event.stopPropagation();
        }
        form.classList.add('was-validated');
      }, false);
    });
  }, false);

    var number1 = document.querySelector('.count-1'),
      numberTop1 = number1.getBoundingClientRect().top,
      start1 = 0, end1 = 92;

    window.addEventListener('scroll', function onScroll() {
        if(window.pageYOffset > numberTop1 - window.innerHeight / 2) {
            this.removeEventListener('scroll', onScroll);
            let interval = setInterval(function() {
                number1.innerHTML = ++start1;
                if(start1 == end1) {
                    clearInterval(interval);
                }
            }, 2);
        }
    });
    var number2 = document.querySelector('.count-2'),
      numberTop2 = number2.getBoundingClientRect().top,
      start2 = 910, end2 = 1013;

    window.addEventListener('scroll', function onScroll() {
        if(window.pageYOffset > numberTop2 - window.innerHeight / 2) {
            this.removeEventListener('scroll', onScroll);
            let interval = setInterval(function() {
                number2.innerHTML = ++start2;
                if(start2 == end2) {
                    clearInterval(interval);
                }
            }, 1);
        }
    });
    var number3 = document.querySelector('.count-3'),
      numberTop3 = number3.getBoundingClientRect().top,
      start3 = 35000, end3 = 35097;

    window.addEventListener('scroll', function onScroll() {
        if(window.pageYOffset > numberTop3 - window.innerHeight / 2) {
            this.removeEventListener('scroll', onScroll);
            let interval = setInterval(function() {
                number3.innerHTML = ++start3;
                if(start3 == end3) {
                    clearInterval(interval);
                }
            }, 1);
        }
    });
    var number4 = document.querySelector('.count-4'),
      numberTop4 = number4.getBoundingClientRect().top,
      start4 = 98580, end4 = 98675;

    window.addEventListener('scroll', function onScroll() {
        if(window.pageYOffset > numberTop4 - window.innerHeight / 2) {
            this.removeEventListener('scroll', onScroll);
            let interval4 = setInterval(function() {
                number4.innerHTML = ++start4;
                if(start4 == end4) {
                    clearInterval(interval4);
                }
            }, 1);
        }
    });
    var number5 = document.querySelector('.count-5'),
      numberTop5 = number5.getBoundingClientRect().top,
      start5 = 0, end5 = 1;

    window.addEventListener('scroll', function onScroll() {
        if(window.pageYOffset > numberTop5 - window.innerHeight / 2) {
            this.removeEventListener('scroll', onScroll);
            let interval = setInterval(function() {
                number5.innerHTML = ++start5;
                if(start5 == end5) {
                    clearInterval(interval);
                }
            }, 1);
        }
    });

})();

jQuery(document).ready(function ($) {

	$(".slider-link").click(function() {
	    var location = window.location.href;
    	var cur_url = '/' + location.split('/').pop();
    	var link = $(this).find('a').attr('href');
 
        if (cur_url == link)
        {
            $(this).addClass('active');
        }
	});

	$(document).ready(function () {
	    var hash = window.location.hash;
	    if (hash === "#thank-you") {
	        $('#thakYouModal').modal('show');
	    }
	});

});